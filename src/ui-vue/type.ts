// ui/button
export enum nativeButtonTypes {
  Button = 'button'
}
export type nativeType = 'button';

export enum functionButtonTypes {
  Text = 'text',
  Main = 'main',
  Second = 'second',
  Sub = 'sub',
}
export type functionType = 'text' | 'main' | 'sub' | 'second';

export enum buttonSizes {
  Mini = 'mini',
  Small = 'small',
  Medium = 'medium',
  Large = 'large',
  Huge = 'huge',
  Super = 'super',
  Full = 'full',
  Fit = 'fit',
}

export type buttonSize = 'mini' | 'small' | 'medium' | 'large' | 'huge' | 'super' | 'full' | 'fit';
